{
    "tasks": [
        {
            "label": "qt_debug_build",
            "type": "shell",
            "command": "${workspaceRoot}/.vscode/debug_build.sh",
            "args": ["${file}"],
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ],
    "version": "2.0.0"
}